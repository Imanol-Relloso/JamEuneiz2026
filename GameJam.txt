Clase GatoBarco{

ATRIBUTOS:
	[SF] bool errorEnNomb
	[SF] bool errorEnBandera
	[SF] bool errorEnCiudad
	.
	.
	.


	+enum gatoNombre = antonio
	+enum gatoPais = pakistan
	+enum gatoCiudad

	+enum barcoPais --> miauland
	+enum barcoCiudad
	+bool cargaPeligrosa
	+bool barcoPirata
	+enum tipoDeCarga

	+enum dialogociudad

FUNCIONES:
	start() --> setters	

	+Seters de dialogo (si errorNom == true --> )

	+setters de barco (si sale que tiene x bandera --> aparece Sprite con esa carga)
	
	
	+los getters

	+bool Acertado() ()

	+bool ComprobarNombre()
	.
	.
	.
}


clase GameManager{
	
	
}



clase libro{

ATRIBUTOS:
	-int pagActual
	-sprite[] paginas

FUNCIONES:
	+void mostrarLibro() (que al pulsar tab muestre el libro)

	-void mostrarPag() (que se muestre la pagina del libro que toca)
	+void sigPag() (cambiar la pagina a la siguiente)
	+void antPag() (cambiar la pagina a la anterior)

}